(function(pjs){
   var def = {
     template: {
        url: '/components/iteratorDemo/iterator.demo.html'
     },
     methods: {
     },
     events: {

     }
   }

   window.iteratorDemo = pjs.createComponent(def);
})(window.pjs);
